package model.npcs.behaviors;

import model.Actor;
import model.GameData;

public class ConcreteReadyForCommandsBehavior implements ReadyForCommandsBehavior {
    @Override
    public void act(Actor npc, GameData gameData) {

    }
}
